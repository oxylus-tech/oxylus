/**
 * This module provide Oxylus' main elements and utilities that are used
 * to run an application inside the framework.
 *
 * Note: the components are in a separate library (`ox/components`).
 */
export {default as config} from './config'

// export {default as App} from './app'
export * from './composables'
export * from './utils'
export * as models from './models'
